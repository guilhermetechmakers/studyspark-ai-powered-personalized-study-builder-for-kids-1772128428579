/**
 * ErrorPage - 500 Server Error page.
 * Renders ServerErrorPageContainer with retry and contact support actions.
 */

import { ServerErrorPageContainer } from '@/components/server-error'

export function ErrorPage() {
  return <ServerErrorPageContainer />
}

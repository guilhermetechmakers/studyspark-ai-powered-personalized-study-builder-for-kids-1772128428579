/**
 * NotFoundPage - 404 Not Found page.
 * Renders NotFoundPageContainer with auth-aware recovery paths.
 */

import { NotFoundPageContainer } from '@/components/not-found'

export function NotFoundPage() {
  return <NotFoundPageContainer />
}
